# Example configuration for development profile
spring:
  profiles: dev
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/iamarpitnamdeo/config-repo
server:
  port: 8888

